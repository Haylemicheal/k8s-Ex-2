apiVersion: v1
kind: Service
metadata:
  name: temperature-converter-backend-lb
  labels:
    app: temperature-converter-backend
spec:
  type: LoadBalancer
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: temperature-converter-backend

