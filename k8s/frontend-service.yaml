apiVersion: v1
kind: Service
metadata:
  name: temperature-converter-frontend
  labels:
    app: temperature-converter-frontend
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  selector:
    app: temperature-converter-frontend

